import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'staff-sync',
  webDir: 'dist/employeemanagementUI'
};

export default config;
